# Ledger service

PORT=9094

DB_HOST=localhost
DB_PORT=3306
DB_USER=go_user
DB_PASSWORD=go123
DB_NAME=ledger_db

JWT_SECRET=paylater-super-secret-key

# Upstream domain services (purchase / payback)
CUSTOMER_SERVICE_URL=http://localhost:9093
MERCHANT_SERVICE_URL=http://localhost:9092

# Shared service-to-service token sent on all internal HTTP calls. Must match other services.
INTERNAL_SERVICE_TOKEN=paylater-internal-dev-token
