apiVersion: v1
kind: Service

metadata:
  name: report
  namespace: paylater

spec:
  selector:
    app: report

  ports:
    - protocol: TCP
      port: 9095
      targetPort: 9095

  type: ClusterIP