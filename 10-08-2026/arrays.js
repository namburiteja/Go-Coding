const customers = [
    {name : "teja", amount : 25000},
    {name : "sai", amount : 18000}
]
length = customers.length
for (let i=0; i<length; i++){
    console.log(customers[i].name, customers[i].amount)
}   