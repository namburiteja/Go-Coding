apiVersion: v1
kind: Service

metadata:
  name: customer
  namespace: paylater

spec:
  selector:
    app: customer

  ports:
    - protocol: TCP
      port: 9093
      targetPort: 9093

  type: ClusterIP